% Phantom Codes — main paper master file (JAMIA submission).
%
% Pure-LaTeX source. Section prose lives in sections/*.tex; xelatex +
% biber compiles directly with no pandoc step. To rebuild after
% editing any section:
%
%     cd paper && make phantom_codes
%
% Output: paper/phantom_codes/build/main.pdf (snapshotted to
%         paper/phantom_codes.pdf via `make snapshot-phantom_codes`).
%
% Editing model:
%   - All prose edits happen in sections/*.tex.
%   - This main.tex only changes when you add/remove/reorder sections,
%     change document class, add/remove packages, or restyle the title.

\documentclass[11pt]{article}

% ─── Fonts & Unicode (XeLaTeX) ─────────────────────────────────────────
% Build engine is xelatex (see paper/Makefile). fontspec switches to
% OpenType font handling; STIX Two Text is a Times-like OpenType font
% with broad Unicode coverage that is available on texlive-basic
% installs (some Latin Modern OTF variants are not). Mathematical
% glyphs (≥, ≤, →, ≈) are written in source as $\geq$, $\leq$, etc.
\usepackage{fontspec}
\setmainfont{STIX Two Text}

% ─── Layout & typography ───────────────────────────────────────────────
\usepackage[margin=1in]{geometry}
\usepackage{microtype}                  % subtle kerning improvements
\usepackage{parskip}                    % paragraphs separated by blank lines

% ─── Tables, figures, lists ────────────────────────────────────────────
\usepackage{graphicx}
\usepackage{booktabs}                   % \toprule/\midrule/\bottomrule
\usepackage{longtable}                  % multi-page tables
\usepackage{array}
\usepackage{caption}
% Smaller, italic caption text with a bold "Figure N:" label, so
% captions visually separate from body prose. Standard scientific
% convention.
\captionsetup{font={small,it}, labelfont={small,bf,up}, labelsep=colon}

% JAMIA heading hierarchy: BOLD CAPS, bold lower case, Plain text,
% Italics (per General Instructions for Authors). Top-level section
% headings are written in uppercase directly in section sources so
% they render as BOLD CAPS without depending on titlesec (not in
% texlive-basic). Subsections inherit the default bold sentence-case;
% sub-subsections are plain; paragraphs are italic.
\usepackage{calc}                       % column-width expressions in tables

% ─── Math ──────────────────────────────────────────────────────────────
\usepackage{amsmath}
\usepackage{amssymb}                    % \mathbb, \leq, \geq, etc.

% ─── Bibliography ──────────────────────────────────────────────────────
% biblatex with biber backend — modern Unicode-friendly citation engine.
% Sections use \autocite{Key}; biblatex resolves at build time against
% references.bib using the style configured below.
%
% Citation style options (one-line swap between venues):
%   - vancouver:    superscripted   — biomedical (NEJM AI, JAMIA, BMJ) ← active
%   - numeric-comp: [1, 2]          — IEEE, Nature, NeurIPS
%   - authoryear:   (Devlin, 2019)  — skim-friendly drafting style
%
% Active style: vancouver (AMA-family, superscripted numeric refs).
\usepackage[backend=biber, style=vancouver, sortlocale=en_US,
            maxnames=3, minnames=3]{biblatex}
\addbibresource{../references.bib}

% ─── Links & references ────────────────────────────────────────────────
\usepackage[colorlinks=true,
            linkcolor=blue!60!black,
            citecolor=blue!60!black,
            urlcolor=blue!60!black]{hyperref}
\usepackage{xurl}                       % wrap long URLs

% ─── Code & quotes ─────────────────────────────────────────────────────
\usepackage{xcolor}
\usepackage{fancyvrb}                   % code blocks
\usepackage{framed}                     % blockquote backgrounds
\usepackage{csquotes}

% ─── Review-mode niceties (commented; uncomment for NEJM AI submission) ─
% Line numbers throughout the body — most biomedical journals (NEJM AI
% included) want these on the submitted PDF so reviewers can reference
% specific lines. Comment-out for read-throughs; uncomment for submission.
% \usepackage{lineno}
% \linenumbers
% Double-spacing — JAMIA submission convention.
\usepackage{setspace}
\doublespacing

% ─── Pandoc legacy shim ────────────────────────────────────────────────
% \tightlist still appears in section sources (it suppresses inter-item
% spacing in lists). Define it so the document compiles standalone.
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

\begin{document}

% ─── Title page ────────────────────────────────────────────────────────
% Content lives in sections/title_page.tex; this file provides the
% titlepage environment + single-spacing wrapper so the section file
% can stay focused on JAMIA-required content.
\begin{titlepage}
\thispagestyle{empty}
\begin{singlespace}
\input{sections/00_title_page}
\end{singlespace}
\end{titlepage}

% ─── Abstract ──────────────────────────────────────────────────────────
% Structured abstract on its own page. Content lives in
% sections/abstract.tex; see that file for the JAMIA-required
% subsection structure and word-count cap.
\input{sections/01_abstract}

\newpage

% ─── Body ──────────────────────────────────────────────────────────────
% Each section is a hand-edited .tex file under sections/. Add new
% sections by: (1) creating sections/NN_name.tex and (2) appending an
% \input{sections/NN_name} line below.

\input{sections/02_background_and_significance}
\input{sections/03_materials_and_methods}
\input{sections/04_results}
\input{sections/05_discussion}
\input{sections/06_conclusion}

\bigskip
\hrule
\bigskip

% ─── Back-matter (JAMIA order) ─────────────────────────────────────────
% JAMIA General Instructions order: Acknowledgments, Competing
% Interests, Funding, then other required statements (Data
% availability, Code availability, Author contributions). Reference
% list follows.

\section*{ACKNOWLEDGMENTS}
\textbf{Use of AI tools.} This work used Anthropic's Claude (Claude
Code, model Claude Opus 4.7) as a coding, drafting, and
figure-design assistant during the development of the benchmark
codebase, the manuscript prose, and the figures. The author
conceived and designed the study, defined the experimental framework
and outcome taxonomy, conducted all analyses, and reviewed and
verified the AI-generated content. The author retains full
responsibility for the content, methodology, results, and any
errors. 

\section*{COMPETING INTERESTS}
The author declares no competing interests.

\section*{FUNDING}
This work received no external funding. LLM API access (Anthropic,
OpenAI, Google) for the evaluation matrix was self-funded by the
corresponding author; per-call costs are itemized in §Results.

\section*{DATA AVAILABILITY}
The Synthea-generated FHIR Bundles used for the headline evaluation
are freely redistributable and will be released as
\texttt{benchmarks/synthetic\_v1/} alongside the paper at
\url{https://github.com/ufbfung/phantom-codes}. All source code,
including the data-preparation pipeline, degradation logic,
ICD-10-CM validator, eval runner, prompts, and baselines, is
publicly available at the same repository under the MIT License.

\section*{CODE AVAILABILITY}
All source code, configuration files, synthetic test fixtures, and
the manuscript source are publicly available at
\url{https://github.com/ufbfung/phantom-codes} under the MIT
License. The repository includes the full evaluation pipeline and
the Synthea generation configuration needed to reproduce the
headline matrix end-to-end.

\section*{AUTHOR CONTRIBUTIONS}
B.K.F.\ designed the benchmark, implemented the codebase, conducted
the experiments, and wrote the manuscript.

\bigskip
\hrule
\bigskip

% Auto-generated bibliography — biblatex pulls cited entries from
% references.bib, formats per the style configured in the preamble.
\section*{REFERENCES}
\printbibliography[heading=none]

\end{document}
